language: php
php:
  - '7.0'
  - '7.1'
  - '7.2'

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --prefer-source --no-interaction

before_install:
  - mysql -e 'CREATE DATABASE edu_app;USE edu_app;'
  - mysql -u root --password="" < tests/travis.sql

script:
  - vendor/bin/codecept run

services:
  - mysql

notifications:
  email: false